1) Пройти тест;

2) Откорректировать базу данных, проблема с миграциями;
- Поднять контейнер PostgreSQL;

- Проверить корректность моделей;

- Проверить текущие миграции в папке migration/versions;
- Удалить лишние миграции в случае их наличия;

- Сгенерировать новые миграции командой:
alembic revision --autogenerate -m 'Type of creater_id changed' ;

- Накатить миграции командой:
alembic upgrade head;

3) Реализовать клиентский запрос по созданию комнаты;

4) Протестировать функционал от клиента до БД.
